/**
 * Lint de l'API NestJS (Node, CommonJS en sortie, syntaxe ESM en source).
 * Réutilise la config partagée de la racine.
 */
import { wilinwiConfig } from '../../eslint.base.mjs';

export default wilinwiConfig();
